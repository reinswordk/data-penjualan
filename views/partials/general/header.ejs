<!-- Header -->
<div class="header pb-8 pt-5 pt-lg-8 d-flex align-items-center" style="min-height: 600px; background-image: url(<%- __siteurl %>/img/theme/profile-diansastro.jpg); background-size: cover; background-position: center top;">
  <!-- Mask -->
  <span class="mask bg-gradient-dark opacity-8"></span>
  <!-- Header container -->
  <div class="container-fluid d-flex align-items-center">
    <div class="row">
      <div class="col-lg-12">
        <h1 class="display-2 text-white"><%= (locals.page && locals.page.title) || 'This Page is Still Under Construction' %></h1>
        <p class="text-white mt-0 mb-5"><%= (locals.page && locals.page.subtitle) || 'Please be patient, sometimes being with me is unbearable' %></p>
      </div>
    </div>
  </div>
</div>